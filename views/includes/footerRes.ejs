<footer>
    <div style="height: 100px;">
    </div>
    <% if(currUser && !currUser.owner._id.equals(currUser._id) && currUser.type==="Restaurant") { %>
    <nav class="navbar navbar-expand-lg fixed-bottom navbar-custom">
        <div class="container-fluid d-flex justify-content-between align-items-center">
            <a class="filter nav-link" href="/restaurant/<%= currUser.id %>/show" aria-label="Home">
                <i class="fa-solid fa-house"></i>
                <p>Home</p>
            </a>
            <a class="filter nav-link" href="/restaurant/<%=currUser.id%>/order" aria-label="Orders">
                <i class="fa-solid fa-bag-shopping"></i>
                <p>Orders</p>
            </a>
            <a class="filter nav-link" href="/restaurant/<%=currUser.id%>/statistics" aria-label="My Cart">
                <i class="fa-solid fa-chart-line"></i>
                <p>statistics</p>
            </a>
            <!-- <a class="filter nav-link" href="/<%= currUser._id %>/cart" aria-label="My Cart">
                <i class="fa-solid fa-utensils"></i>
                <p>Profile</p>
            </a> -->
        </div>
    </nav>
    <% } %>
</footer>
